/**
 * The lazy entry point for the integrations screen.
 *
 * A separate file from `Integrations.tsx` so the glob in `../index.ts` has one
 * predictable filename to look for in every feature.
 */
import { Integrations } from './Integrations';

export default Integrations;
